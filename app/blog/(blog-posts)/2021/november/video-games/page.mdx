import { fetchManifest } from '@lib/fetch-manifest';
import { formatBlogPostMetadata } from 'lib/formatMetadata';
import BlogPage from 'app/blog/BlogPage';

export let id = '13297295850138705032';

export async function generateMetadata() {
  let mainfest = await fetchManifest();
  let post = mainfest.posts.find(p => p.uuid === id);

  return formatBlogPostMetadata({ meta: post });
}

<BlogPage id={id}>


I've been thinking a bit about video games as of late, for those that don't know
I play a decent amount of video games, its practically the second largest time
sink of my day following work. I mostly play on Xbox (I was fortunate enough to
be able to get a Series X and a PS5 a year ago when they came out), but have a
Switch and a PC (as well as a recent Stadia subscription).

Lately I've been playing the two largest triple-A titles that usually come out
frequently, these

<Spacer />

---

<Spacer />


</BlogPage>